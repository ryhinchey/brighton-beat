---
title: The Brighton Beat Live
---
<section class="live">
    <%= partial 'header' %>
    <div class="live-primary">
        <h1> The Brighton Beat Show Archive </h1>
        <small> * All shows stream/download for free * </small>
    </div>
        <div class="show-list">

            <ul>
                <% data.shows.shows.each do |f| %>
                    <li> <a href=<%= f.link %>> <%= f.name %></a> - <span><%= f.type %></span></li>

                <% end %>
            </uL>



        </div>
    </section>
